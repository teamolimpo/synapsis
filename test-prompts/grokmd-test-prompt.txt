**Synapsis + Grok Build Integration Test (New Session)**

You are starting a completely fresh session inside the `synapsis` project.

The project now has a proper integration surface beyond just a single GROK.md:
- `AGENTS.md` (standard project rules entry point) + `.grok/rules/*.md`
- `.grok/skills/handoff/` and `.grok/skills/synapsis/` (slash commands: /handoff, /synapsis)
- `.grok/hooks/synapsis-hygiene.json` (auto hygiene on Stop/PreCompact)
- The detailed manual is still in `GROK.md`

Your job is to execute a small, self-contained verification task **while using the modern integration surface** (prefer skills when they apply) and strictly following the discipline described in AGENTS.md + GROK.md.

### The Task

Perform a "health check + documentation" of the current synapsis memory setup in this project, and record everything properly.

You must do the following steps (in roughly this order):

1. **Start correctly**
   - Initialize a synapsis session with a clear topic like: "GROK.md integration test - verifying correct synapsis usage and handoff discipline in Grok Build".
   - Use `resume=true`.

2. **Explore the current state**
   - Use `search` (with different scopes and layers) to understand what is currently stored (sessions, tasks, observations, handoffs, entities, etc.).
   - Also pull stats using the admin tool.

3. **Track the work**
   - Create 1-2 tasks for this verification effort (e.g. one for "Explore current memory state", one for "Produce handoff and verify discipline").

4. **Capture progress with observations**
   - Record at least 3 structured observations during the process (mix of `decision`, `note`, and `result`).
   - Use `entities` where relevant.
   - Link observations to tasks using `tref` when appropriate.

5. **Mandatory formal handoff** (this is the most important part)
   - Before finishing, produce **at least one real handoff** using `synapsis__hf` with `act="new"`.
   - The handoff must document:
     - Current state of the synapsis store
     - How well the new GROK.md instructions appear to be working
     - Any interesting findings or recommendations about memory usage
   - Include a small `## Wiki` section in the body (even if minimal).
   - After creating the handoff, log it on the relevant task using `task act=log` with `evt="handoff_ref"` and the `hpath`.

6. **Demonstrate good recall habits**
   - Perform at least one search with `l=1`, one with `l=2`, and (optionally) one with `l=3` or using `tk`.
   - Show that you can target specific scopes (tasks, timeline, hf, etc.).

7. **Close the loop properly**
   - Run a dry consolidate (`consolidate` with `auto=true, dry=true`).
   - Pull fresh stats with admin at the end.
   - Optionally run a session summarize.

### Rules you must follow (from AGENTS.md + GROK.md)

- Prefer the project skills when applicable: `/handoff` and `/synapsis` (they encode the correct sequences).
- When using raw tools: always discover with `search_tool` (query containing "synapsis"), then `use_tool` with qualified names only (`synapsis__session`, `synapsis__hf`, etc.).
- Respect `dry=true` on operations that support it.
- Link things (entities, tref, hpath).
- "Handoff before you return control" — formal handoff (via skill or raw) is not optional.
- Narrate clearly, referencing AGENTS.md / GROK.md / the skills.

### Output at the end

When you are finished, give me a clear summary containing:

- The `sid` of the session you worked in
- The task ID(s) you created
- The handoff `ref` you generated (e.g. hf-xxxx)
- The full relative path of the handoff file that was written on disk (check Library/Handoff/...)
- Whether any Wiki contribution was created
- A short list of the main things you recorded in synapsis during this test
- Any observations about whether the GROK.md instructions were easy/hard to follow

Start now. Be methodical and transparent about every tool call. Do not just describe what you would do — actually call the tools.